# Resume point, 2026-09-06 (evening)

Read `handoff/HANDOFF-2026-09-06.md` first — it is the substantive handoff for
this day (post-fix re-measure, per-row topology, the ICNN topology sweep, and
the open ALE-ratio discrepancy). `NUMBERS-2026-09-05.md` stays canonical for
values. This file is only the delta written after that one was pushed.

## 1. Figure 3B PPO encoder — array RESUBMITTED (this supersedes "queue empty")

`HANDOFF-2026-09-06.md` §7 says the cluster queue is empty. It is not any more.

Job 44559823 was never a timeout. Tasks 4-7 all died at
`2026-09-05T09:53:48` on SIGTERM and 8-12 were killed while still pending —
one `scancel` over the whole array. Tasks 1-3 (`breakout.multi`, 3 seeds)
completed cleanly at 150M steps and their outputs are good.

**Resubmitted 2026-09-06 as job 44806731**, `--array=4-12%4`:
`qbert.v2` x3, `flappy_bird.dunk2` x3, `frostbite.jungle` x3.

- Walltime raised 4h -> **8h**. `qbert.v2` runs at roughly half
  `breakout.multi`'s throughput (2,525 of 6,103 updates in 1h52 against 6,103
  in 3h10), so it would not have fit in the original `-t 0-04:00` even without
  the cancel. `ppo_variants.sbatch` on disk still says 4h and the 8h is a
  command-line override, so **pass `-t 0-08:00` again on any resubmit**, or
  bake it into the script.
- Four partial output dirs were moved aside so the reruns start clean:
  `outputs/_ppovar/stale_44559823/{ppo_impala_qbert_v2_s0,_s1,_s2,ppo_impala_flappy_bird_dunk2_s0}`.
  Delete that directory once the new runs land.
- When it lands: repoint Figure 3B's PPO arm off `ppo_dirs()` -> `pv_p_*`
  (Nature-CNN) at `outputs/ppo_impala_<game>_s<seed>/` and re-render. See
  `HANDOFF-2026-09-05-figure-consistency.md` §3.

Check with `fasrc 'sacct -j 44806731 --format=JobID%20,State,Elapsed,ExitCode -n'`.

Also queued and unrelated to this: `icnn_topo_sweep.sbatch` job 44806296,
submitted 09:33, array 0-3%4 at 1h50 each — that is the §3 sweep.

## 2. The ALE ratio discrepancy is CLOSED, and Table 13 is seven-point

`HANDOFF-2026-09-06.md` §4 leaves the ALE 80-thread ratio open with three
candidate values and calls it a blocker on Tables 11 and 13. It has since been
settled in favour of that section's own independent recompute, and the edits are
pushed (ICLR `12a4fc9`, then `39a62bb`). Verified in the tree:

| place | now reads |
|---|---|
| `main.tex:541` prose | 20.80x ALE, 2.58x ProcGen |
| `main.tex:1492` Table 11 thread-scaling row | 20.80x / 2.58x |
| `main.tex:1562` Table 13 ProcGen 80 | 3,650,005 / 1,412,903 = 2.58x |
| `main.tex:1571` Table 13 ALE 80 | 7,300,384 / 350,959 = 20.80x |

Table 13 now carries all seven thread points, matching Figure 4A. 20.59x and the
bare 2.57x appear nowhere in `main.tex` any more, and prose and table agree. So
§4's "must be settled before those tables are written" is done — do not reopen it.

Still open in that neighbourhood: **Table 9 (`tab:dbuf-ablation`)** needs its
re-based per-game values pulled and its caption's topology claim resolved — the
caption says Nature ran at Table 8's topology (12 workers) while Table 1(a)'s
Nature row used 15. The re-base measured 1.25x at 15 workers and 1.36x at 12
against a published 1.35x, so if the 12-worker caption is right the number
barely moves.

For the record, the rest of the morning `RESUME-2026-09-06.md` swap list has
landed and was verified in the tree: L503 `1.07M`, L582 `1.07~M` with "16 out of
the 24", L765 `miner` rewritten at `75\%` of step (the "one ProcGen game our
replica steps more slowly" clause is gone), L766 `10\%` and `18\%`, L767
`79--390~ns`.

**The ICLR repo is at `39a62bb`. Pull in Overleaf.**

## 3. Human wall-clock, L706-707 — still stale

`playtrain-trainers` cf85c6a substituted suite curves for the six shared games,
which moves the §4.3 crossings, but `main.tex` L706-707 still reads the old
values ("10~M steps on `asteroids` and 20~M on `seaquest`", then 86~M / 79~M /
16~M).

New: `asteroids` IMPALA 10.2M -> **26.3M**, PPO 7.1M -> **68.2M**; `breakout`
PPO never -> **68.2M**; `seaquest` IMPALA 19.9M -> **30.5M** (final 528 -> 715);
`coinrun` 78.7M -> **81.2M**; `plunder` 15.8M -> **16.1M**.

Two flips are knife-edge rather than learning differences — PPO's final return
sits within a few percent of the human line on `asteroids` (658 vs 652) and
`breakout` (264 vs 242) — so consider reporting final returns alongside
crossings. Original data kept at `rerun_curves_icnn.json.bak`.

## 4. Figures, unchanged and still open

- **Ladder rung mismatch, undecided.** The figure computes 498 / 4.6k for the
  ProcGen browser and Node rungs from the committed
  `results/env_throughput/backend_ladder_fasrc.json`; `NUMBERS-2026-09-05.md`
  lists 477 / 4,081. The QuickJS rung agrees. Decide which source the caption
  quotes.
- The main composite's panel C now reads `_suite4_curves.json` (paper e58d3d6),
  so Figure 3C and the appendix suite figures plot the same runs. The composite
  still needs re-rendering wherever the panel A/B frame assets live.
- `downwell` (Figure 1A): a scan of `outputs/*/config.json` finds no IMPALA-CNN
  runs, yet the main text quotes ~300 PPO / ~170 IMPALA. Locate those runs
  before deciding whether it needs a re-run.

## 5. Repo state — untracked work is now committed

`HANDOFF-2026-09-06.md` §7 notes `tools/replay-video.mjs` had uncommitted local
edits that were deliberately left alone. Those are committed now (`a68349c`),
along with everything else that had never been tracked, so a fresh clone on
another machine has the full picture:

- `tools/replay-video.mjs` — the two frame-skip bugs found via the flappy clips
  on 09-04. Per-episode `frameSkip`/`maxSteps` instead of one session-wide
  value, and a capture stride divided by `frameSkip` so an action-repeat clip is
  not decimated twice. Only `flappy_bird` and `flappy_bird.dunk2` trained at
  skip>1, so these are the only clips affected.
- `website/` — the playtrain.org project page, its docs sub-site, rollout GIFs
  and figures. `website/site/` is build output and is now gitignored; regenerate
  with `bash website/build.sh`.
- `ACTION_SPACE_PLAN.md`, `CONTINUOUS_INPUT_PLAN.md`, `PLAYTRAIN_3D_PLAN.md`,
  `tools/build-embed.mjs`, `games/js/asteroids.dash.js`,
  `games/js/frostbite.v2.js`.

**`.github/workflows/site.yml` is deliberately still untracked.** It deploys
`website/site` to GitHub Pages on every push to main touching `website/**`, and
we are not turning that on yet. It sits in the working tree when you want it —
committing that one file is the whole switch.

Branches `action-spaces`, `continuous-input`, `engine-tier` and `native-tuning`
still hold their own work; engine-tier L1 is already merged to main.
