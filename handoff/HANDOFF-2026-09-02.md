# Handoff, 2026-09-02 (laptop, pre-office-move) — adv re-measure cascade

Eighth handoff. Written before a machine move; everything here is state the
office machine needs. Follows HANDOFF-2026-09-01 (EnvPool matrix DONE, Fig 4A
redesign decisions) — that file still stands; this one records the EXECUTION of
the adoption cascade after **dv was adopted as `adv`** (build job 43780730,
`variants/qjs_host.adv` md5 `46ea4999...`, `variants/libqjs_vec.adv.so` md5
`b3709b39...`, worktree `playtrain-wt-tuning`).

Everything below ran under `QJS_DIRTY=1` on the PlayTrain/QuickJS arms
(dirty-skip is inside the measurement claims, per the adoption decision).

## 1. DONE: single-core panels C/D + backend ladder, re-measured under adv

All three jobs COMPLETED on **holy8a32607** (the published panel-C node), each
echoing `hostname` and the adv md5, both arms inside the one `-c 1` sapphire
job, methodology otherwise byte-identical to the published sweeps
(`benchmarks/as_run/` in this repo).

| job | what | log |
|---|---|---|
| 43782780 | setup: backed up old `qjs_host` -> `qjs_host.pre-adv` (md5 `3527d381...`), installed adv into `~/node-gym-smoke/node-gym/native/build/qjs_host` | `~/adv_setup.out` |
| 43783363 | panel C: QuickJS-adv vs ProcGen, 16 games, 7x100k steps + frames 1500/warmup 200/trials 7 | `~/sweep4_adv.out` |
| 43783364 | panel D: QuickJS-adv vs ALE, 8 games, same shape | `~/sweep6_adv.out` |
| 43783367 | ladder: Playwright -> Node/V8 -> QuickJS-adv, 24 games, one job | `~/sweep7_adv.out` |

(`~` = `/n/home06/truong` throughout.)

**Panel C (mean of game means: 42,555 vs 22,734 = 1.87+-0.01x; QJS trial CV 0.21-2.29%):**

```
game             QuickJS mean+-std      ProcGen mean+-std   ratio
plunder           124224 +-   772        37563 +-   347  3.31+-0.04
bigfish           108716 +-   392        37442 +-  1956  2.90+-0.15
ninja              98694 +-   239        18169 +-   572  5.43+-0.17
bossfight          84856 +-  1144        14977 +-   956  5.67+-0.37
starpilot          63416 +-   384        34836 +-   506  1.82+-0.03
leaper             39883 +-   200        25999 +-  3074  1.53+-0.18
heist              26278 +-   198        24587 +-  1691  1.07+-0.07
dodgeball          19447 +-    41        25876 +-  1559  0.75+-0.05
maze               17947 +-   410        12272 +-   926  1.46+-0.12
jumper             17727 +-    90        16104 +-   353  1.10+-0.02
caveflyer          17575 +-    53        16114 +-   553  1.09+-0.04
chaser             15544 +-    97        26217 +-   149  0.59+-0.01
climber            13741 +-    43        22841 +-  1775  0.60+-0.05
fruitbot           12193 +-    35        14351 +-    41  0.85+-0.00
coinrun            11074 +-    39        21951 +-  2780  0.50+-0.06
miner               9567 +-    25        14454 +-   176  0.66+-0.01
```

**Panel D (geomean 57,113 vs 7,034 = 8.1x over the 6-game table; freeway +
asteroids are in the raw files as before):**

```
qbert     11337+-54   / 5703  = 2.0x      space_invaders 62841+-276 / 9628 = 6.5x
seaquest  82197+-605  / 9297  = 8.8x      frostbite      52651+-305 / 5196 = 10.1x
pong     312610+-2434 / 11721 = 26.7x     breakout       36008+-398 / 3896 = 9.2x
```

**Ladder (24 games): Playwright 502 -> V8 4,374 -> QuickJS-adv 37,350;
PW->V8 8.7x, V8->QJS 8.5x, PW->QJS 74x** (was 30.5k QJS / 6.2x / 59x published).

Raw outputs, all `_adv`-suffixed (published cluster files untouched):
`~/qjs_raw4_adv.txt ~/procgen4_adv.json ~/qjs_atari6_adv_raw.txt
~/ale_atari6_adv.json ~/compare_atari6_adv.json ~/pw_fasrc_adv.json
~/node_pg_adv.json ~/node_at_adv.json ~/qjs_fasrc_adv.txt
~/backend_ladder_fasrc_adv.json`. Scripts: `~/{adopt_setup_adv.sh,
sweep4_adv.sh,sweep6_adv.sh,sweep7_adv.sh,pw_bench_fasrc_adv.mjs}`.

**Deviations from published methodology** (all recorded, none load-bearing):
`--dependency=afterok:43780730` was unusable (build already left the queue) —
ordering enforced by md5 verification instead; the three sweeps ran
concurrently on the shared node (each panel still same-job-both-arms); new
venvs carry newer packages (originals deleted, versions unrecorded); time
limits padded; the Playwright `.mjs` copy differs only in its output filename.

## 2. DONE: the deleted baseline environments, rebuilt

`/n/home06/truong/node-gym-smoke/node-gym` (a symlink to `node-gym-git`):

- `.venv` — uv, **Python 3.12**: gymnasium 1.3.0, ale-py 0.12.1, numpy 2.5.2,
  node-gym editable. Drives the ALE baseline and the V8 arm.
- `.venv-procgen` — uv, **Python 3.10**: procgen 0.10.7, gym 0.23.1 (NOT 0.26 —
  `bench_compare.py` uses the 4-tuple step API), numpy 1.26.4.
- `node_modules` — `npm ci` (canvas 3.2.3 prebuilt, per lockfile) plus
  `npm install --no-save playwright-core@1.57.0`.
- **playwright-core 1.57.0 ships chromium build v1200** — exactly the
  `~/.cache/ms-playwright/chromium-1200/chrome-linux64/chrome` path the as-run
  Playwright bench pins. Version map (via jsdelivr `browsers.json`): 1.54->1181,
  1.55->1187, 1.56->1194, **1.57->1200**. Reinstall recipe if the cache dies
  again: `node node_modules/playwright-core/cli.js install chromium`.

## 3. DONE: replot of the throughput figure (laptop scratchpad — NOT committed)

`playtrain-paper/tools/plot_throughput_all.py` re-rendered twice in a session
scratchpad: once from the committed published data (passes its built-in
checksum -> old data intact), once from the adv data with the checksum relaxed
to a note. Visual: every PlayTrain bar up (pong 168k->313k, plunder 95k->124k),
baselines unchanged, ladder 30.5k->37.3k.

To redo anywhere: stage the five `_adv` files from §1 under the names
`qjs_raw4.txt procgen4.json qjs_atari6_raw.txt ale_atari6.json
backend_ladder_fasrc.json` in a scratch `results/env_throughput/`, point a
COPY of the script at it, disable the `_EXPECTED` SystemExit.

**On adoption**: update `_EXPECTED_PROCGEN`/`_EXPECTED_ATARI` in BOTH
`playtrain-paper/tools/plot_throughput_all.py` AND `tools/throughput_panels.py`
(the main composite imports the latter), replace the raw files in
`playtrain-paper/results/env_throughput/`, and never ship the relaxed copy.

## 4. IN FLIGHT / FORK: Fig 4A (thread scaling)

Two parallel tracks exist — **decide which one the figure uses**:

**(a) Published-protocol re-run under adv (this session, PENDING):** jobs
**43891521** (procgen) + **43891522** (ale) =
`playtrain-trainers/benchmarks/bench_vec_scaling_ab_adv.sbatch`, a copy of the
as-run `bench_vec_scaling_ab.sbatch` whose entire diff is the `$TMPDIR` shadow
tree (wt-tuning src + `libqjs_vec.adv.so` as its `libqjs_vec.so`), QJS_DIRTY=1,
an md5 provenance gate, and the job name — same mechanism as
`run_procgen_ab_adv16.sh`. EnvPool arm (sync1 protocol) unchanged. Constrained
to the fast **17xxx** kempner nodes via `--exclude` of everything else +17601
(the 1.56x clock trap; published curves were on 17402). Results will land as
`results/ab_{pt,ep}_<game>_<jobid>.json`; the published
`*_{38145651,39032276}.json` are untouched. Replot:
`plot_env_efficiency.py --pg-job 43891521 --ale-job 43891522`.

**(b) Matrix-based redesign (HANDOFF-09-01 §4, mostly DONE):** Fig 4A reports
PT + EnvPool as-shipped (sync1) + documented-best (async+NUMA+affinity). The
EP arms are **binary-independent** (already measured, holy8a24307). The PT arm
is re-anchored under the real adv .so by **adv_anchor 43780731 (COMPLETED,
holy8a24307)** — quotable curve in HANDOFF-2026-09-01 §11 **on main**:
ProcGen16 290,228 / 582,092 / 1,161,420 / 2,325,995 (2.00x per doubling);
ALE8 665,642 / 1,328,324 / 2,654,832 / 5,308,537. The 09-01 §2 affinity gap
is CLOSED: **ep_best_sweep 43779854** (same node) measured documented-best
WITH in-pool affinity down the curve — ProcGen16 355,451 / 587,070 /
931,786 / 1,413,748; ALE8 45,662 / 89,285 / 177,869 / 350,601
(`analogen-jaxbench/outputs/ep_best_sweep_43779854.json`). **That is the
primary EP-curve source.** Final same-node headline ratios (main §11):
ProcGen 1.64x vs documented-best / 5.0x vs as-shipped; ALE 15.1x / 22x.
Note EnvPool-tuned beats adv at 10 threads (355k vs 290k) — the Fig 4A
story is the scaling, not every point.

**CORRECTION / duplication warning:** this session, unaware of 43779854
(recorded only in main's copy of HANDOFF-2026-09-01, §§10-11 — the
native-tuning copy ends at §9), submitted a same-design job
(`analogen-jaxbench/ep_aff_sweep.sbatch`; 44103900, cancelled while stuck
on the 24307 pin, resubmitted UNPINNED as **44113007** (`-C genoa --mincpus=96`;
an earlier unpin, 44110138, landed on a 64-core Genoa node where the
80-thread point would oversubscribe, and was preempted anyway) — so it
doubled as a node-generality check. **REPLICATION CONFIRMED** (44113007
COMPLETED 31 min, holy8a28511, 2x96 NPS2): every geomean point reproduces
43779854 within 1% — ProcGen16 355,085 / 582,206 / 934,231 / 1,403,463
(deltas -0.1% / -0.8% / +0.3% / -0.7%); ALE8 45,657 / 89,187 / 177,593 /
349,427 (all within -0.4%). Output:
`analogen-jaxbench/outputs/ep_aff_sweep_44113007.json`. The tuned-EnvPool
curve is node-robust across the Genoa CPU fleet; 43779854 remains primary. **Before submitting cluster jobs,
read HANDOFF-2026-09-01 §§9-11 on MAIN — the branch copies fork.**

If (b) is final, jobs 43891521/22 are redundant — **cancel at will** (same
verdict 09-01 §7 gave the old pinned pair 43246914/43542348). If the paper
keeps the published figure design, (a) is the like-for-like update.

Also pending, not ours: `ladder_adv15203` 43783186 (analogen-jaxbench tree).

## 5. Cascade scoreboard (from 09-01 §6a: what adoption invalidates)

| item | status |
|---|---|
| Table 1 trainer A/B, ProcGen | re-run 43783180+43783184 COMPLETED (parallel effort, `playtrain-trainers` logs) |
| Table 1 trainer A/B, ALE | re-run 43783185 COMPLETED (ditto) |
| panels C/D (single-core) | **DONE — §1** |
| backend ladder | **DONE — §1** |
| Fig 4A | fork — §4 |
| Table 7 | NOT covered by anything this session — check what it needs |

## 6. Ledger of everything this session touched on the cluster

Created: the §1 scripts + raw files in `~`; `.venv`, `.venv-procgen`,
`node_modules`, `~/.cache/ms-playwright/` under `node-gym-smoke`;
`playtrain-trainers/benchmarks/bench_vec_scaling_ab_adv.sbatch`;
`native/build/qjs_host.pre-adv` (backup) + the adv `qjs_host` in place.
Submitted: 43782780, 43783363/4/7 (done), 43884356/7 (cancelled, were pinned
to busy 17402), 43891521/2 (COMPLETED, holygpu8a17103 — published-protocol
record: PT-adv 2.346M PG / 5.393M ALE at 80t, EP sync1 449k / 254k, both
cross-validating the matrix within ~1%), 44103900/44110138 (cancelled) -> 44113007 (COMPLETED — replication of
43779854 confirmed within 1%, see §4). NOT touched: live `playtrain` tree,
`analogen-jaxbench/.venv` (envpool 1.2.5 verified importable, no `uv sync`
anywhere), jobs 43780730/43780731/43779854 and the parallel effort's jobs.
