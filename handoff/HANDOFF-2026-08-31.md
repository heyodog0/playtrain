
## 7. Companion job (laptop, 2026-09-01): 43542348 on 17402

`analogen-jaxbench/final17402.sbatch`, submitted `-w holygpu8a17402`,
queued BEHIND (not replacing) 43246914, which keeps its queue age and owns
sync16/sync16n. One job, same node, same day, everything else the paper
pass needs:
- PT live / rv2 / dv at published topology (16w x 128e x 5t), all 24
  games, 3 interleaved trials — t11_full methodology, dv with QJS_DIRTY=1.
  Turns the ~2.36M dv projection into a measured 17402 anchor.
  Outputs: `outputs/final17402_pt_43542348/`.
- EP sync1 (published protocol) + EP async+NUMA (documented best)
  same-day re-anchors. Output: `outputs/ep_anchor_43542348.json`.
Worktree default .so is restored to rv2 at the end of the PT block.
The paper pass (§4) now waits on BOTH 43246914 and 43542348.

## 8. Any-node superset (laptop, 2026-09-01): 43543523

Ryan's call: same-node-to-HISTORY is not required, only same-node WITHIN the
comparison — the paper pass replaces every Fig-4A number anyway.
`analogen-jaxbench/final_any.sbatch` (-C genoa --exclusive, NO node pin) is
a superset of both pinned jobs plus the figure curves, all on one node:
PT live/rv2/dv 80th (24 games x 3 trials) + PT live/dv sweep w=2/4/8/16;
EP sync1/sync16/sync16n/async+numa 80th (24 games) + ProcGen16 sweeps at
10/20/40 threads. Outputs: `outputs/final_any_pt_43543523/`,
`outputs/ep_final_any_43543523.json`. The pinned pair (43246914, 43542348)
stays queued as a free 17402 tie-in; whichever lands first unblocks the
paper pass. Losing the "reproduces published 445k within 3.6%" cross-check
is accepted if the any-node job is the one that lands.
