# Resume point, 2026-09-06 — paper edits outstanding

Read `handoff/NUMBERS-2026-09-05.md` first; it is the source of truth for values.
This file is only what is left to DO.

## Paper edits still pending (repo ICLR-PlayTrain-Fast-LLM-VGEs, main.tex)

| line | change |
|---|---|
| 503-509 | Table 1(a): 0.94M/0.35M/171k/64k/0.91M/1.01M -> **1.07M / 0.35M / 185k / 68k / 1.06M / 1.09M** |
| 582 | prose "0.94~M" -> **1.07~M**; recount the "16 of 24 surpass 1M" clause against the new per-game data |
| 1486 | bench-setup thread-scaling row 20.98x/2.58x -> **20.59x / 2.57x** (cross-node values superseded) |
| 1487 | bench-setup trainer row 5x/1.7x -> **5.81x / 2.25x** |
| 764 | delete "miner is the one ProcGen game our replica steps more slowly" — miner is 1.67x in the swap and wins per core |
| 764 | "80\% of its step on 787 drawing commands" -> **75\%** |
| 766 | "0.03 ns against the 98--1304 ns" -> order-of-magnitude phrasing against **79--390 ns** (new pixel fit is weak, R^2 0.85) |
| 766 | dodgeball/climber "15\% and 16\% drawing" -> **10\% and 18\%** |
| tab:bench-scaling | replace body with the tier3 seven-point table; EnvPool column is now the TUNED arm, not as-shipped |

## Owed, not number swaps (NUMBERS-2026-09-05 "Owed to the paper")

1. **Tier sentence.** Every number now comes from the compile-at-load AOT path;
   the text never says so. Claim: tier 2 immediately, tier 3 about a minute
   later, bit-exact, gain 1.05-2.1x, geomean 1.19/1.30 on nine held-out games.
2. **Bossfight footnote.** EnvPool's BossfightEasy reads 78,149 at 80 threads,
   ~20x below every other ProcGen game; excluding it pulls the 80-thread ratio
   from 2.57x toward ~2.1x.
3. **Validation gap** (methodology/limitations): every gate the tier adoption
   passed exercised the SYNC path; the fork host's async entry point was never
   run. That is how a 6x regression reached a headline table.
4. Do NOT write a sentence saying trainer-attached runs use the interpreter
   because AOT breaks double buffering. That regression was fixed on 09-05
   (envs pinned to an owner worker for life) and the paper is now one binary.

## Figures

- `figures/fig_env_efficiency.pdf` is current (commit 2d19e1f): seven thread
  points in A, value labels back in B, no break marker in D.
- Ladder rung mismatch to reconcile: the figure computes 498 / 4.6k for the
  ProcGen browser and Node rungs from the committed
  `results/env_throughput/backend_ladder_fasrc.json`, while NUMBERS lists
  477 / 4,081. QuickJS rung agrees. Decide which source the caption quotes.
- Panel C of the main composite now reads `_suite4_curves.json`, so Figure 3C
  and the appendix suite figures are the same runs (committed, playtrain-paper
  e58d3d6). The composite still needs re-rendering where the panel A/B frame
  assets live.

## Figure 3B, PPO encoder

Job 44559823 (PPO at IMPALA-CNN for breakout.multi, qbert.v2,
flappy_bird.dunk2, frostbite.jungle, 3 seeds) finished **13 COMPLETED, 9
CANCELLED** — incomplete. Re-submit the cancelled tasks before repointing
Figure 3B's PPO arm off `pv_p_*` (Nature-CNN). See
`HANDOFF-2026-09-05-figure-consistency.md` for why that arm is wrong.

## Human wall-clock

`playtrain-trainers` cf85c6a substitutes suite curves for the six shared games.
That moves the section 4.3 crossings: asteroids IMPALA 10.2M -> 26.3M, PPO
7.1M -> 68.2M; breakout PPO never -> 68.2M; seaquest IMPALA 19.9M -> 30.5M.
Main text ~L700 still says 10M and 20M. Two of those flips are knife-edge (PPO
final within a few percent of the human line), so consider reporting final
returns alongside crossings. `rerun_curves_icnn.json.bak` is the original.
