# Handoff, 2026-09-03 (laptop, machine move) — figure previews + full paper re-run batch

Ninth handoff. Builds on HANDOFF-2026-09-02 (this branch) and, crucially,
HANDOFF-2026-09-01 §§9-12 **on main** — the branch copies of 09-01 fork; always
read main's before submitting jobs (that gap caused one duplicated job this
session, kept as a replication).

## 1. In-flight jobs (state at handoff time; monitor dies with the laptop session)

`squeue -u truong` / `sacct -j <id>` to pick up. All run the adv shadow-tree +
md5-gate pattern, QJS_DIRTY=1, nothing shared modified.

| job | measures | pin | state at handoff |
|---|---|---|---|
| 44161947 | Table 1(b) ALE swap absolutes | holygpu8a15204 | PENDING |
| 44162618 | Table 1(a) IMPALA+Nature all-24 (0.94M row; per-suite 0.91M/1.01M rows fall out of its per-game output) | 15204 | PENDING |
| 44162619 | Table 1(a) IMPALA+ICNN all-24 (0.35M row, also fixes its 15-game defect) | 15204 | PENDING |
| 44162620 / 21 | Table 1(a) PPO nature / impala (171k / 64k rows) | 15204 | PENDING |
| 44162628[1-36%8] | fig_human_wallclock re-runs: 6 suite study games x 2 trainers x 3 seeds; configs cloned to `_suite3adv/`, log dirs `s3adv_*` (published TB untouched) | 17xxx class | flowing (task 1 COMPLETED 7:47 on 17304, gate OK, EXIT=0) |

The five 15204-pinned jobs serialize on that node (92-core each; node is
MIXED+PLANNED, not draining). Scripts: `analogen-jaxbench/{suite_mps_adv,
icnn_suite24_adv,ppo_suite24_adv,suite3_adv}.sbatch`,
`playtrain-trainers/benchmarks/baselines/as_run/run_ale_ab_adv.sh` (44161947).

Node policy (Ryan's instruction): exact historical nodes not required, but zero
node variance inside any compared set — hence one node for all of Table 1, one
clock class (17xxx) for the wall-clock training runs. After landing, check the
lscpu MHz line echoed in each log: 15204's clock vs the ProcGen-swap nodes
(13201/15502) before quoting (a) against the ProcGen (b) row in prose.

## 2. Figure preview work (committed in `handoff/fig_preview_2026-09-03/`)

`plot_env_efficiency` was rendered end-to-end with adv data three ways; PNGs +
the patched scripts are committed here (they were scratchpad-only). All bypass
the published-values checksum in a COPY — never ship that; on adoption update
`_EXPECTED_*` in `playtrain-paper/tools/{plot_throughput_all,throughput_panels}.py`
and replace `results/env_throughput/` raw files (staged `_adv` files in
`/n/home06/truong/`).

- `fig_env_efficiency_adv.png` — current script design (panel A = EnvPool
  as-shipped only, data from kempner pair 43891521/22; printout 5.22x/21.19x).
- `fig_env_efficiency_adv_2arm.png` — the 09-01 §4 decided design (both EnvPool
  arms). Caveat: shared y-scale squashes the ProcGen crossover; consider
  per-suite subpanels or log-y if adopted.
- `fig_env_efficiency_adv_bestonly.png` — Ryan's requested variant: EnvPool
  documented-best only, ladder value labels REMOVED (adv ALE bar outgrew the
  published ylim and its label clipped). Ratios: 1.65x PG / 15.14x ALE vs
  tuned; 4.96x/22.23x vs as-shipped in the printout. NOTE: showing best-only
  walks back 09-01 §4 ("report TWO arms") — needs explicit sign-off.
- Panel-A data provenance (hardcoded geomeans, comments in scripts): PT =
  adv_anchor 43780731; EP tuned = ep_best_sweep 43779854 (replicated <1%/point
  by 44113007 on a third node); EP as-shipped = matrix sync1 curve.

Two figure-semantics flags raised this session:
- **"documented best" label**: EnvPool documents NO ProcGen benchmark; suggest
  legend "EnvPool (tuned)" + caption clause (config = their documented async +
  NUMA + thread_affinity_offset + bs swept; Atari recipe transplanted). Must
  not imply best-found (sync16 stays cut).
- **Panel B error bars are cross-game spread** (multiplicative SE of the
  geomean across games), NOT measurement noise (trial CV < 2.3%). They widened
  under adv (pong 313k vs qbert 11k). Caption must say so, or switch to
  per-game dots / drop them; C/D whiskers are per-game trial SE — don't let one
  figure use "whisker" to mean two things silently.

## 3. Main-text edit checklist (stale number -> measured replacement)

| where | says | replace with |
|---|---|---|
| L519 Table 1 PG row | 372k/618k | 372,539 / 701,992 (1.88x) |
| Table 1 ALE row | 175k/873k | from 44161947 (same-silicon 15204) |
| Table 1(a) rows | 0.94M/0.35M/171k/64k/0.91M/1.01M | from 44162618-21 |
| L529 | "seven of the sixteen", 7.0x/1.13x | ten of sixteen, 8.6x / 1.35x (geomeans) |
| L530 | "remaining nine" | remaining six |
| L532 | 6x V8, 59x browser | 8.5x, 74x |
| L536-38 | "15 of 16 ... 1.7x ... miner 0.81x" | swap data: all 16 ratios >1 now (miner 1.09x) — recheck wording |
| L540 | "15x ALE and 4x ProcGen at eighty threads" | 15.1x / 1.64x vs tuned (best-only design) |
| L570 | dbuf "large part" | Table 7 re-base: 1.25x (15w) / 1.36x (12w topology) |
| L1368 + L1397/1406 appendix table | 4.00x/15.50x, 1.78M/3.95M | kempner pair 43891521/22: 5.22x/21.19x, 2.35M/5.39M |
| fig_human_wallclock | wall-clock axis from live-binary runs | rebuild from s3adv_* runs when 44162628 lands |

Footing warning: 8.6x/1.35x are geomeans (match the figure's mean bar); the
1.87x in sweep4_adv.out is an arithmetic mean of game means — don't mix.

## 4. Findings this session (no job needed)

- 09-01 §11's "31/32, one ProcGen pair failed verification" is FALSE: all 32
  `logs/pgadv_*.verdict` PASS; the summaries differ only by write timing
  (43783184 snapshotted ~3s before fruitbot landed). Nothing to re-run.
- EnvPool tuned curve replicated within 1% per point on a third node
  (44113007, holy8a28511) — node-robust; 43779854 stays primary.
- The kempner published-protocol pair (43891521/22, both suites one node
  17103, gate OK) maps 1:1 onto the appendix scaling table, NOT main Fig 4A.

## 5. Open items

1. fig_human_wallclock: flappy_bird + vvvvvv configs are outside the suite3
   manifest — locate and re-run if their curves must reflect adv.
2. qbert retrain-vs-footnote (style-cache fix postdates published curves) —
   replaces published data; Ryan's sign-off required.
3. Panel-A design (two-arm vs best-only) + "tuned" label + panel-B whisker
   treatment — Ryan's calls, then the one-shot figure/prose pass (main §12.1).
4. Build policy: portable vs PGO artifact (unchanged from 09-01 §6b).
5. Table 1(a) OPEN question from main §10 (24-game suite grid) is now CLOSED —
   running as 44162618-21.
