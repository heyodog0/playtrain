# Handoff, 2026-09-01 (laptop) — the EnvPool matrix is DONE

Seventh handoff. Supersedes HANDOFF-2026-08-31 §§1-4 (the EnvPool question)
and its §7-9 addenda. Ryan is writing the appendix; this file carries every
number and decision the writing needs. All jobs below ran on ONE node
(holy8a24307, 192-core Genoa) unless noted — every ratio is same-silicon.

## 1. The scoreboard (80 env threads, geomeans, frame_skip=1, 64x64 RGB)

ProcGen16:

| arm | steps/s |
|---|---|
| PlayTrain dv (tuned, QJS_DIRTY=1) | **2,359,959** |
| PlayTrain rv2 | 2,294,321 |
| [EnvPool sync16+bind — CUT from paper, archived control] | 2,007,342 |
| PlayTrain live (published binary) | 1,776,929 |
| EnvPool async+NUMA+affinity (documented best) | **1,413,748** |
| EnvPool async+NUMA, numactl only | 1,284,141 |
| EnvPool sync1 + affinity | 577,487 |
| EnvPool sync1 (as-shipped protocol) | 468,997 |

ALE8: PT dv 5,350,956 / PT live 3,876,774 / EP best 350,601 / EP sync1 238,829.

Headline ratios (pending adoption decision):
- dv:   **1.67x** ProcGen / **15.3x** ALE vs documented-best; 5.0x / 22x vs as-shipped.
- live: 1.26x ProcGen / 11.1x ALE vs documented-best.
- live measured 1.777M here vs 1.798M on 17402 — the two nodes agree within
  ~1%, so any-node was the right call.

## 2. Scaling curves (the new Fig 4A data)

ProcGen16 geomeans by env threads (10/20/40/80):

- PT dv:        294,105 / 590,052 / 1,179,606 / 2,358,521  (2.00x per doubling — linear)
- PT live:      222,345 / 444,453 /   889,150 / 1,773,705  (linear)
- EP async+numa:256,559 / 453,385 /   788,923 / 1,284,141  (sublinear, 1.63-1.77x)
- EP sync1:     178,217 / 278,042 /   417,106 /   468,997  (flattens hard)

ALE8 (job 43574839 + 80th from matrix):

- PT dv:        670,250 / 1,340,327 / 2,672,933 / 5,350,956  (linear)
- PT live:      486,468 /   970,384 / 1,940,970 / 3,876,774  (linear)
- EP async+numa: 42,806 /    85,179 /   171,400 /   342,334
- EP sync1:      38,478 /    73,884 /   141,421 /   238,829

NOTE: sweep curves were measured WITHOUT affinity for EP (affinity landed
after). The 80th documented-best point with affinity is 1.414M; if the
figure plots the async+numa curve, either re-sweep 10/20/40 with affinity
(one ~20-min job) or plot the numactl curve and put the affinity best as a
point/footnote. Do not mix silently.

## 3. What changed the EnvPool story (chronology of controls)

1. Published Fig-4A arm (sync1, one pool, one driver) was partly a driver
   artifact — it flattens because ONE Python process pumps 2,048 envs.
2. `thread_affinity_offset` (documented envpool.make arg, default OFF) is
   REAL: +23% on sync1, +10% on async+numa (ProcGen). In-pool affinity
   beats external numactl for their async path. EnvPool documented-best
   ProcGen = 1.414M (job 43570992).
3. ep_knobs (43576663, DONE): both remaining knobs are null or worse.
   bs sweep under affinity: bs32 787k (their documented value — much
   worse), bs60 1.392M, bs120 1.413M (best; reproduces the affinity
   probe within 0.03%), bs240 1.410M. Pool depth 4096: 1.358M — no
   headroom. **EnvPool tuned is FINAL: 1.414M ProcGen.** Ratios in §1
   are settled.
4. EnvPool documents NO ProcGen benchmark at all (their benchmark page is
   Atari/Mujoco/dm_control) — our "documented best" is already a generous
   transplant of their Atari recipe.
5. Their "1M FPS" README headline counts skipped frames (Pong, frameskip
   4 = 250k env-steps/s). All our numbers are frame_skip=1 on both sides.

## 4. Reporting decisions ALREADY MADE (Ryan, this session)

- Fig 4A reports TWO EnvPool arms: as-shipped (sync1) and documented-best
  (async+NUMA+affinity family). NOT best-found.
- sync16/sync16n is CUT from the paper entirely — archived control. Its
  2.007M is a FLOOR (never got affinity). Consequence: the kicker sentence
  ("their engine is fastest when rearranged into our shape") CANNOT be
  used in the paper — it cites the cut measurement. Handoff/docs-site only.
- No EnvPool build dependence for PlayTrain. Rationale (for app:backend, 2
  sentences max): we adopt the architecture (batched in-process stepping,
  no Python in the loop, cite weng2022envpool) but not the implementation —
  each env is pinned to one thread with no shared queue, because at
  PlayTrain per-env speeds a shared dispatcher saturates first (supported
  by the REPORTED arms alone: sync1 caps ~0.5M under both APIs).

## 5. Measurement methodology (for the app:bench prose)

- One harness shape for every arm: build vec env, warm up (20 batched
  steps sync / 4s async), 12-second timed window, random actions,
  completed env-steps / wall time. Sync counts iters x num_envs; async
  counts returned env-ids. PT arm: bench_vec_rollout, 300 steps x N envs,
  best of 3 trials (2 for sweeps), resets included.
- Matched observation product everywhere: 64x64 RGB, stack 1, frame_skip 1
  (EP ALE forced off its 84x84-gray-stack4 default).
- EnvPool reported at the best configuration across its documented
  settings (config matrix = their own num_envs-sweep practice).
- The +-15% driver caveat belongs to the SINGLE-CORE panels only (three
  different harnesses there); the vec matrix is one harness shape.
- Remaining asymmetry, DECISION not measurement: build provenance (dv =
  PGO/-march tuned vs EnvPool's generic manylinux wheel). Options:
  headline the portable build (tipplain +21%), or source-build EnvPool
  (half-day, Bazel), or shipped-vs-shipped caption clause.

## 6. Open decisions (all Ryan's, now with full data)

a. ADOPTION: live / rv2 / dv. dv => "ahead under every configuration";
   live => 1.26x vs documented-best (and BEHIND the archived control).
   Any change from live invalidates: Table 1 both halves, panels C/D
   (qjs_host single-core), backend ladder (6.2x/59x move with the tuned
   qjs_host), Table 7. Cascade list in HANDOFF-2026-08-31 + §5 above.
b. BUILD POLICY: portable (tipplain, no PGO) vs tuned (dv, needs
   wt/pgo/*.profdata) as the paper's benchmark artifact. Interacts with
   the provenance asymmetry in §5.
c. DIRTY-SKIP POLICY: dv includes adaptive render memoization — bit-exact
   outputs (gates ran with dirty FORCED ON), changes only cost. Inside or
   outside the paper's measurement claims?

## 7. Job ledger (2026-09-01, all holy8a24307 except noted)

| job | what | state |
|---|---|---|
| 43543523 | final_any matrix: PT live/rv2/dv + EP 4 arms + sweeps | DONE |
| 43570992 | thread_affinity probe | DONE — knob is real |
| 43574839 | ALE 10/20/40 sweep, PT + EP | DONE |
| 43576663 | ep_knobs: bs x affinity, pool depth | DONE — null, bs120 optimal |
| 43246914 / 43542348 | pinned 17402 pair | PENDING — now REDUNDANT, cancel at will |

Outputs: `outputs/final_any_pt_43543523/`, `outputs/ep_final_any_43543523.json`,
`outputs/ep_affinity_43570992.json`, `outputs/ale_sweep_pt_43574839/`,
`outputs/ep_ale_sweep_43574839.json`, `outputs/ep_knobs_43576663.json`.
Aggregation: geomean over games of max-over-trials of `frames_per_s`
(JSONs may be lists — take last element).

## 8. Writing queue (unchanged items from 08-31 still stand)

1. app:playwright_node_qjs — notes delivered in chat (three-rung setup,
   the production-path nuance for the middle rung, ±15% caveat, browser
   arm is multi-process/per-core note). Unblocked.
2. app:bench / Fig 4A five-point pass — write ONCE after 43576663 lands
   and decisions a/b/c are made. §2 note about the affinity-vs-sweep
   mismatch applies.
3. app:backend — gate sentence still owed; add the 2-sentence EnvPool
   design rationale (§4).
4. Empty captions (p5 subset table, 3 suite figs, trainer timeline, plus
   the commented drafts at the bench tables).
5. app:variants — still blocked on rename-sweep decision.
6. qbert/aim_trainer determinism holes — RESOLVED (d46fcae main,
   ef74835 native-tuning). Root cause: the native p5 style cache —
   afterFill() re-validated stale stroke mirrors after pop(), so the next
   stroked draw skipped setting the rasterizer's stroke/width. Fix =
   shim-exact mirror nulling. gate_qjs.sh --all: 100/100 on both
   lineages; the bit-identical claim now has zero known divergences.

## 9. ADOPTION DECIDED: dv (Ryan, 2026-09-02)

Decision (a) is made: the paper moves off the live binary to **dv**
(rv2 recipe + hygiene rasterizer + dirty-skip host, QJS_DIRTY=1). This
implicitly decides (c): dirty-skip is inside the measurement claims
(bit-exact, gate-verified with dirty forced on). Decision (b) — portable
vs PGO-tuned build as the released artifact — is STILL OPEN.

Consequences now in motion:
- 43779854 ep_best_sweep (on 24307): EnvPool documented-best curve
  (async+NUMA+affinity) at 10/20/40 threads, ProcGen16+ALE8 — the final
  EnvPool arm for Fig 4A. 80th points already exist (1.414M / 351k).
- The adoption cascade, in order:
  1. Merge native-tuning -> main (dv code + the determinism fix d46fcae
     already on both), re-cut the dv binary FROM MAIN on the cluster so
     the measured artifact includes the style-cache fix.
  2. Re-measure with the new binary: Table 1 both halves (trainer
     pipeline A/Bs), panels C/D (tuned qjs_host single-core sweeps),
     backend ladder (all three arms, one job), Table 7 dbuf re-base.
  3. THEN the five-point Fig 4A / lines 540-541 / Tables 7-8 paper pass,
     written once. Headline ratios (same-node): dv 1.67x ProcGen / 15.3x
     ALE vs EnvPool documented-best; 5.0x / 22x vs as-shipped.

## 10. Cascade results, wave 1 (2026-09-02, adv binary = merged main + fresh PGO)

Build 43780730 DONE (adv variants in worktree; qjs_host.adv md5 46ea4999...,
libqjs_vec.adv md5 b3709b39...). Panels + ladder re-measured on holy8a32607
(the published panel-C node), all arms same-job, zero errors, raw outputs
*_adv-suffixed in /n/home06/truong/ (published files unclobbered):

- Panel C (per-core vs ProcGen, 16 games): 42,555 vs 22,734 = **1.87x** mean
  (published 1.47x; QuickJS was 33.0k). Recompute the per-game win count
  from qjs_raw4_adv.txt before touching the "seven of the sixteen" prose.
- Panel D (per-core vs ALE): geomean 57,113 vs 7,034 = **8.1x** (was 6.95x).
- Backend ladder: 502 -> 4,374 -> 37,350; PW->V8 8.7x, **V8->QJS 8.5x**
  (was 6.2x), end-to-end **74x** (was 59x). Main text line ~532 and
  tab:backend-ladder's comment facts must update in the paper pass.
- Bench venvs rebuilt in ~/node-gym-smoke/node-gym (.venv py3.12
  gymnasium/ale-py; .venv-procgen py3.10 procgen 0.10.7 + gym 0.23.1);
  chromium build v1200 restored via playwright-core 1.57.0 — the published
  Playwright script's pinned path, original code unmodified.

Still in flight: 43780731 adv_anchor (Fig 4A PT curve, matrix node),
43783180/43783184 (Table 1(b) ProcGen swap, H100), 43783185 (ALE swap),
43783186 (Table 7 re-base, pinned 15203). 43779854 ep_best_sweep DONE —
read outputs/ep_best_sweep_43779854.json for the final EnvPool curve.
OPEN: whether Table 1(a)'s 24-game suite grid (suite_mps family, ~1 day
GPU) joins the cascade.
