# Figure 4B, Playwright and Node/V8 rungs (job 43783367)

One core, 24 games, 500 frames. `backend_ladder_fasrc_adv.json` holds the geomeans
(Playwright 502, Node/V8 4,374), recomputed exactly from `pw_fasrc_adv.json` and
`node_{pg,at}_adv.json`. `qjs_fasrc_adv.txt` is the QuickJS rung at the same build;
the figure's QuickJS rung (58,827) is the later build, job 44515373.
